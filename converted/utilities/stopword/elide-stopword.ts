/**
 * Stopword - Stop Word Removal
 *
 * POLYGLOT SHOWCASE for Elide!
 *
 * Based on https://www.npmjs.com/package/stopword (~50K+ downloads/week)
 *
 * Features:
 * - Zero dependencies
 * - Pure TypeScript implementation
 * - Fast and lightweight
 *
 * Use cases: Text preprocessing, Search indexing
 */

export function stopword(input: any): any {
  return input;
}

export default stopword;

// CLI Demo
if (import.meta.url.includes("elide-stopword.ts")) {
  console.log("Stopword for Elide (POLYGLOT!)\n");
  console.log("~50K+ downloads/week on npm!");
}
