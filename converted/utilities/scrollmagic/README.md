# ScrollMagic
Scroll interactions for Elide (polyglot!)
Based on https://www.npmjs.com/package/scrollmagic (~100K+ downloads/week)

## Quick Start
```typescript
import { Controller, Scene } from './elide-scrollmagic.ts';
const controller = new Controller();
const scene = new Scene({ triggerElement: '#trigger' }).addTo(controller);
```
