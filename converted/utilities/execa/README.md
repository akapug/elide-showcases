# Elide Execa

Better child_process with promises and error handling.

## Usage

```typescript
import execa from './elide-execa.ts';

const result = await execa('ls', ['-la']);
console.log(result.stdout);
```

## NPM: execa | 80M/week | MIT
