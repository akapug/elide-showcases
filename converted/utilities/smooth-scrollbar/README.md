# Smooth Scrollbar
Smooth scrollbar for Elide (polyglot!)
Based on https://www.npmjs.com/package/smooth-scrollbar (~100K+ downloads/week)

## Quick Start
```typescript
import Scrollbar from './elide-smooth-scrollbar.ts';
const scrollbar = Scrollbar.init(element, { damping: 0.1 });
```
