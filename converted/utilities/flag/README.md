# Flag - Simple Flag System - Elide Polyglot Showcase

> **Simple flags for ALL languages** - TypeScript, Python, Ruby, and Java

## 🚀 Quick Start

```typescript
import { createFlagSystem } from './elide-flag.ts';

const flags = createFlagSystem();
flags.set('feature-a', true);
if (flags.get('feature-a')) {
  // Feature enabled
}
```

**npm**: ~5K+ downloads/week

---

**Built with ❤️ for the Elide Polyglot Runtime**
