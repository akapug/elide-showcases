# Email Domain Validator - Email Domain Validation Library

Validate email domains with DNS and MX checks.

## Quick Start

```typescript
import { validateEmailDomain, isValidDomain } from './elide-email-domain-validator.ts';

const result = validateEmailDomain("user@example.com");
const domainValid = isValidDomain("example.com");
```
