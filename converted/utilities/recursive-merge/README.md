# Recursive Merge - Deep Recursive Merging

Recursively merge objects and arrays on Elide.

## Quick Start

```typescript
import recursiveMerge from './elide-recursive-merge.ts';

const merged = recursiveMerge(obj1, obj2, obj3);
```

Based on https://www.npmjs.com/package/recursive-merge (~20K+ downloads/week)
