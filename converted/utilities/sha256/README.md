# SHA256 - Elide Polyglot Showcase

> **One SHA-256 library for ALL languages**

Secure SHA-256 hashing for security-critical applications.

## 🚀 Quick Start

```typescript
import { sha256, sha256Base64 } from './elide-sha256.ts';

const hash = await sha256('hello');
const base64 = await sha256Base64('hello');
const checksum = await sha256(fileContent);
```

## 📝 Package Stats

- **npm downloads**: ~5M/week
- **Use case**: Security, integrity, digital signatures
- **Algorithm**: SHA-256 (cryptographically secure)

---

**Built with ❤️ for the Elide Polyglot Runtime**
