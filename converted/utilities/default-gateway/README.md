# default-gateway - Get Default Gateway - Elide Polyglot Showcase

Get the default network gateway address.

## 🚀 Quick Start

```typescript
import { v4 } from './elide-default-gateway.ts';

const gateway = await v4();
console.log(`Gateway: ${gateway.gateway}`);
console.log(`Interface: ${gateway.interface}`);
```

**Package has ~15M+ downloads/week on npm**
