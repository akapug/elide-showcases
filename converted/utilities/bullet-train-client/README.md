# Bullet Train Client - Feature Flags - Elide Polyglot Showcase

> **Bullet Train for ALL languages** - TypeScript, Python, Ruby, and Java

## 🚀 Quick Start

```typescript
import { createClient } from './elide-bullet-train-client.ts';

const client = createClient({ environmentID: 'env-123' });
await client.init();
if (client.hasFeature('new-ui')) {
  // Show new UI
}
```

**npm**: ~5K+ downloads/week

---

**Built with ❤️ for the Elide Polyglot Runtime**
