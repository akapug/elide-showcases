# Feature Flag - Basic Flags - Elide Polyglot Showcase

> **Basic feature flags for ALL languages** - TypeScript, Python, Ruby, and Java

## 🚀 Quick Start

```typescript
import { createFlags } from './elide-feature-flag.ts';

const flags = createFlags({ 'dark-mode': true });
if (flags.get('dark-mode')) {
  // Enable dark mode
}
```

**npm**: ~5K+ downloads/week

---

**Built with ❤️ for the Elide Polyglot Runtime**
