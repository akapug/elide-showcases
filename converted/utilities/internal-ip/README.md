# internal-ip - Get Internal IP - Elide Polyglot Showcase

Get your internal/local IP address on the network.

## 🚀 Quick Start

```typescript
import { v4 } from './elide-internal-ip.ts';

const localIP = v4();
console.log(`Internal IP: ${localIP}`);
```

**Package has ~8M+ downloads/week on npm**
