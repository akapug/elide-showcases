# React Particles
Particles for React on Elide (polyglot!)
Based on https://www.npmjs.com/package/react-particles (~50K+ downloads/week)

## Quick Start
```typescript
import Particles from './elide-react-particles.ts';
const particles = new Particles({ id: 'particles' });
```
