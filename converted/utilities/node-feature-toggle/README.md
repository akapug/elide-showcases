# Node Feature Toggle - Server-Side Flags - Elide Polyglot Showcase

> **Server feature toggles for ALL languages** - TypeScript, Python, Ruby, and Java

## 🚀 Quick Start

```typescript
import { create } from './elide-node-feature-toggle.ts';

const features = create({ 'api-v2': true });
if (features.isEnabled('api-v2')) {
  // Use API v2
}
```

**npm**: ~5K+ downloads/week

---

**Built with ❤️ for the Elide Polyglot Runtime**
