/**
 * GraphqlDepthLimit - GraphQL Utility
 *
 * A utility for GraphQL applications.
 * **POLYGLOT SHOWCASE**: One implementation for ALL languages on Elide!
 *
 * Package has ~3M/week on npm!
 */

export function createGraphqlDepthLimit(options?: any) {
  return options;
}

if (import.meta.url.includes("elide-graphql-depth-limit.ts")) {
  console.log("🔧 GraphqlDepthLimit - GraphQL Utility (POLYGLOT!)\n");
  console.log("🚀 ~3M/week on npm");
}
