# Cache-Directory - Get Cache Directory

OS-specific cache directory resolution.

Based on [cache-directory](https://www.npmjs.com/package/cache-directory) (~50K+ downloads/week)

## Quick Start

```typescript
import cacheDirectory from './elide-cache-directory.ts';

console.log(cacheDirectory('myapp')); // ~/.cache/myapp
```
