# Flipper - Feature Flipper - Elide Polyglot Showcase

> **Feature flipping for ALL languages** - TypeScript, Python, Ruby, and Java

## 🚀 Quick Start

```typescript
import { createFlipper } from './elide-flipper.ts';

const flipper = createFlipper();
flipper.enable('new-ui');
if (flipper.isEnabled('new-ui')) {
  // Show new UI
}
```

**npm**: ~5K+ downloads/week

---

**Built with ❤️ for the Elide Polyglot Runtime**
