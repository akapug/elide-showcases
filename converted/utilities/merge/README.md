# Merge - Object Merging Utility

Deep merge of objects and arrays on Elide.

## Quick Start

```typescript
import merge from './elide-merge.ts';

const merged = merge(obj1, obj2, obj3);
```

Based on https://www.npmjs.com/package/merge (~100K+ downloads/week)
