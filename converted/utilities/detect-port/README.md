# detect-port - Detect Available Port - Elide Polyglot Showcase

Detect an available port automatically.

## 🚀 Quick Start

```typescript
import detectPort from './elide-detect-port.ts';

const port = await detectPort(3000);
console.log(`Server will use port: ${port}`);
```

**Package has ~15M+ downloads/week on npm**
