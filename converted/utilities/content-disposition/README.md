# Content-Disposition

Content-Disposition for Elide (polyglot!)

Based on https://www.npmjs.com/package/content-disposition (~10M+ downloads/week)

## Features

- Header creation
- Filename encoding
- UTF-8 support
- RFC compliance
- Zero dependencies

## Quick Start

```typescript
import content-disposition from './elide-content-disposition.ts';

// Basic usage
const result = content-disposition.main();
console.log(result);
```

## Polyglot Examples

### JavaScript/TypeScript
```typescript
import content-disposition from './elide-content-disposition.ts';
const result = content-disposition.main();
```

### Python (via Elide)
```python
from elide_content-disposition import content-disposition
result = content-disposition.main()
```

### Ruby (via Elide)
```ruby
require 'elide/content-disposition'
result = content-disposition.main
```

### Java (via Elide)
```java
import elide.content-disposition.*;
String result = Content_Disposition.main();
```

## Benefits

- One library for ALL languages on Elide
- Consistent API across languages
- ~10M+ downloads/week on npm!
