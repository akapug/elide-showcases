# Elide P-All

Pure TypeScript implementation of `p-all`.

## Original Package

- **npm**: `p-all`
- **Downloads**: ~10M/week

## Usage

```typescript
import pAll from './elide-p-all.ts';

const results = await pAll([
  fetchData(1),
  fetchData(2),
  fetchData(3),
]);
```
