/**
 * Marked Utility (re-exported from conversions)
 *
 * Provides Markdown parsing for email templates and product descriptions.
 * Shared across all services.
 */

export { marked, parse } from '../../../conversions/marked/elide-marked.ts';
