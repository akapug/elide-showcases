/**
 * Decimal.js Utility (re-exported from conversions)
 *
 * Provides arbitrary-precision decimal arithmetic for accurate price calculations.
 * Essential for e-commerce to avoid floating-point rounding errors.
 * Shared across all services.
 */

export { Decimal } from '../../conversions/decimal/index.ts';
